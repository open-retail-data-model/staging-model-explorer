# GENERATED by tools/generate_odcs.py — do not edit by hand.
# ODCS v3.1.0 (Bitol / Linux Foundation). OPTIONAL integration; ORDM's
# source of truth is the committed DDL / checks.sql / glossary.md.
# Regenerate: python tools/generate_odcs.py
apiVersion: v3.1.0
kind: DataContract
id: ordm-core-store
name: ordm_store
version: 1.0.0
status: draft
domain: store
tags:
- ordm
- retail
- canonical-core
description:
  purpose: ORDM canonical-core domain 'store'. Generated from the committed DDL, relationships, quality
    checks and glossary — ORDM's source of truth. ODCS is an optional integration; the model does not
    depend on it.
  usage: Optional. Consumable by contract-driven tools (e.g. DQX for data-quality execution, Lockstep
    for Unity Catalog sync, Ontos as data products). See docs/odcs-integration.md.
authoritativeDefinitions:
- url: canonical-core/store/README.md
  type: businessDefinition
- url: canonical-core/store/glossary.md
  type: businessDefinition
- url: docs/data-model-standards.md
  type: businessDefinition
schema:
- name: store
  physicalName: store.store
  physicalType: table
  description: ORDM canonical-core store/location master (SCD2). Conformed store entity shared across
    outcome packages.
  tags:
  - master
  authoritativeDefinitions:
  - url: canonical-core/store/README.md
    type: businessDefinition
  - url: canonical-core/store/glossary.md
    type: businessDefinition
  - url: docs/data-model-standards.md
    type: businessDefinition
  properties:
  - name: store_sk
    logicalType: integer
    physicalType: BIGINT
    required: true
    primaryKey: true
    primaryKeyPosition: 1
    description: Surrogate key. Unique per SCD2 version; the join/FK target.
  - name: store_id
    logicalType: string
    physicalType: STRING
    required: true
    description: Durable natural/business key for the store.
  - name: gln
    logicalType: string
    physicalType: STRING
    required: false
    description: GS1 Global Location Number (13 digits) for the store location.
  - name: store_name
    logicalType: string
    physicalType: STRING
    required: false
    description: Display name of the store.
  - name: store_format
    logicalType: string
    physicalType: STRING
    required: false
    description: 'Format. Allowed values: hypermarket, supermarket, convenience, drugstore, online.'
    quality:
    - type: text
      dimension: validity
      severity: error
      description: 'Allowed values: hypermarket, supermarket, convenience, drugstore, online.'
  - name: region
    logicalType: string
    physicalType: STRING
    required: false
    description: Sales region / area.
  - name: district
    logicalType: string
    physicalType: STRING
    required: false
    description: District within the region.
  - name: city
    logicalType: string
    physicalType: STRING
    required: false
    description: City / locality of the store.
  - name: state_province
    logicalType: string
    physicalType: STRING
    required: false
    description: State, province, or region code.
  - name: country_code
    logicalType: string
    physicalType: STRING
    required: false
    description: Country, ISO 3166-1 alpha-2 code.
  - name: open_date
    logicalType: date
    physicalType: DATE
    required: false
    description: Date the store opened (ISO 8601 date).
  - name: store_status
    logicalType: string
    physicalType: STRING
    required: false
    description: 'Lifecycle status. Allowed values: active, inactive, closed.'
    quality:
    - type: text
      dimension: validity
      severity: error
      description: 'Allowed values: active, inactive, closed.'
  - name: effective_from_date
    logicalType: date
    physicalType: DATE
    required: true
    description: 'SCD2: inclusive start date this version became effective.'
  - name: effective_to_date
    logicalType: date
    physicalType: DATE
    required: false
    description: 'SCD2: exclusive end date; NULL for the current version.'
  - name: is_current
    logicalType: boolean
    physicalType: BOOLEAN
    required: true
    description: 'SCD2: TRUE for the currently active version.'
  - name: created_timestamp
    logicalType: date
    physicalType: TIMESTAMP
    required: false
    description: When the record was created in the source system (UTC, ISO 8601).
  - name: source_updated_timestamp
    logicalType: date
    physicalType: TIMESTAMP
    required: false
    description: When the record was last modified in the source system (UTC, ISO 8601).
  - name: record_source
    logicalType: string
    physicalType: STRING
    required: false
    description: Originating system of record (vendor-neutral label).
  - name: load_timestamp
    logicalType: date
    physicalType: TIMESTAMP
    required: false
    description: When this row was loaded into the canonical core (UTC, ISO 8601).
  quality:
  - name: store_keys_not_null
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: store_keys_not_null
    query: 'SELECT * FROM store.store

      WHERE store_sk IS NULL OR store_id IS NULL OR is_current IS NULL'
  - name: store_business_key_unique_current
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: store_business_key_unique_current
    query: 'SELECT store_id, COUNT(*) AS current_versions

      FROM store.store

      WHERE is_current = true

      GROUP BY store_id

      HAVING COUNT(*) > 1'
  - name: store_format_domain
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: store_format_domain
    query: "SELECT * FROM store.store\nWHERE store_format IS NOT NULL\n  AND store_format NOT IN ('hypermarket',\
      \ 'supermarket', 'convenience', 'drugstore', 'online')"
  - name: store_status_domain
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: store_status_domain
    query: "SELECT * FROM store.store\nWHERE store_status IS NOT NULL\n  AND store_status NOT IN ('active',\
      \ 'inactive', 'closed')"
  - name: store_country_code_iso
    type: sql
    dimension: accuracy
    severity: warning
    mustBe: 0
    unit: rows
    description: store_country_code_iso
    query: 'SELECT * FROM store.store

      WHERE country_code IS NOT NULL AND LENGTH(country_code) <> 2'
