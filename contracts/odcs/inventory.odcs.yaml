# GENERATED by tools/generate_odcs.py — do not edit by hand.
# ODCS v3.1.0 (Bitol / Linux Foundation). OPTIONAL integration; ORDM's
# source of truth is the committed DDL / checks.sql / glossary.md.
# Regenerate: python tools/generate_odcs.py
apiVersion: v3.1.0
kind: DataContract
id: ordm-core-inventory
name: ordm_inventory
version: 1.0.0
status: draft
domain: inventory
tags:
- ordm
- retail
- canonical-core
description:
  purpose: ORDM canonical-core domain 'inventory'. Generated from the committed DDL, relationships, quality
    checks and glossary — ORDM's source of truth. ODCS is an optional integration; the model does not
    depend on it.
  usage: Optional. Consumable by contract-driven tools (e.g. DQX for data-quality execution, Lockstep
    for Unity Catalog sync, Ontos as data products). See docs/odcs-integration.md.
authoritativeDefinitions:
- url: canonical-core/inventory/README.md
  type: businessDefinition
- url: canonical-core/inventory/glossary.md
  type: businessDefinition
- url: docs/data-model-standards.md
  type: businessDefinition
schema:
- name: inventory_position
  physicalName: inventory.inventory_position
  physicalType: table
  description: ORDM canonical-core daily inventory position fact (product x store x day).
  tags:
  - transactional
  authoritativeDefinitions:
  - url: canonical-core/inventory/README.md
    type: businessDefinition
  - url: canonical-core/inventory/glossary.md
    type: businessDefinition
  - url: docs/data-model-standards.md
    type: businessDefinition
  properties:
  - name: inventory_position_sk
    logicalType: integer
    physicalType: BIGINT
    required: true
    primaryKey: true
    primaryKeyPosition: 1
    description: Surrogate key for the daily position record.
  - name: inventory_position_id
    logicalType: string
    physicalType: STRING
    required: true
    description: Durable natural/business key (date|store|product).
  - name: date_key
    logicalType: date
    physicalType: DATE
    required: true
    description: Snapshot date. Logical reference to fiscal_calendar.date_key (ISO 8601).
  - name: product_sk
    logicalType: integer
    physicalType: BIGINT
    required: false
    description: FK to product.product_sk (version-specific).
    relationships:
    - to: product.product_sk
      type: foreignKey
      customProperties:
      - property: toSchema
        value: product
      - property: enforced
        value: false
  - name: product_id
    logicalType: string
    physicalType: STRING
    required: true
    description: Durable product business key.
  - name: store_sk
    logicalType: integer
    physicalType: BIGINT
    required: false
    description: FK to store.store_sk (version-specific).
    relationships:
    - to: store.store_sk
      type: foreignKey
      customProperties:
      - property: toSchema
        value: store
      - property: enforced
        value: false
  - name: store_id
    logicalType: string
    physicalType: STRING
    required: true
    description: Durable store business key.
  - name: units_on_hand_sod
    logicalType: number
    physicalType: DECIMAL(12,3)
    required: false
    description: Units on hand at start of day.
  - name: units_on_hand_eod
    logicalType: number
    physicalType: DECIMAL(12,3)
    required: false
    description: Units on hand at end of day.
  - name: units_in_transit
    logicalType: number
    physicalType: DECIMAL(12,3)
    required: false
    description: Units in transit from DC or supplier.
  - name: units_on_order
    logicalType: number
    physicalType: DECIMAL(12,3)
    required: false
    description: Units ordered but not yet received.
  - name: units_received_today
    logicalType: number
    physicalType: DECIMAL(12,3)
    required: false
    description: Units received today.
  - name: is_stockout
    logicalType: boolean
    physicalType: BOOLEAN
    required: false
    description: Flag indicating if the product was out of stock.
  - name: stockout_pct_day
    logicalType: number
    physicalType: DECIMAL(5,4)
    required: false
    description: Percentage of day the product was out of stock.
  - name: size_availability_pct
    logicalType: number
    physicalType: DECIMAL(5,4)
    required: false
    description: Availability of sizes for this style.
  - name: replenishment_flag
    logicalType: boolean
    physicalType: BOOLEAN
    required: false
    description: Flag indicating a replenishment order was triggered.
  - name: record_source
    logicalType: string
    physicalType: STRING
    required: false
    description: Originating system of record (vendor-neutral label).
  - name: load_timestamp
    logicalType: timestamp
    physicalType: TIMESTAMP
    required: false
    description: Timestamp this row was loaded into the canonical core (UTC, ISO 8601).
  quality:
  - name: inv_pos_pk_unique
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: inv_pos_pk_unique
    query: 'SELECT inventory_position_sk

      FROM inventory.inventory_position

      GROUP BY inventory_position_sk

      HAVING COUNT(*) > 1'
  - name: inv_pos_grain_unique
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: inv_pos_grain_unique
    query: 'SELECT product_id, store_id, date_key

      FROM inventory.inventory_position

      GROUP BY product_id, store_id, date_key

      HAVING COUNT(*) > 1'
  - name: inv_pos_product_fk_orphan
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: inv_pos_product_fk_orphan
    query: 'SELECT i.* FROM inventory.inventory_position i

      LEFT JOIN product.product p ON i.product_sk = p.product_sk

      WHERE p.product_sk IS NULL AND i.product_sk IS NOT NULL'
  - name: inv_pos_store_fk_orphan
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: inv_pos_store_fk_orphan
    query: 'SELECT i.* FROM inventory.inventory_position i

      LEFT JOIN store.store s ON i.store_sk = s.store_sk

      WHERE s.store_sk IS NULL AND i.store_sk IS NOT NULL'
  - name: inv_pos_nonnegative_units
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: Physical inventory cannot be negative.
    query: 'SELECT * FROM inventory.inventory_position

      WHERE units_on_hand_eod < 0 OR units_on_hand_sod < 0'
  - name: inv_pos_stockout_pct_domain
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: percentage domain must be between 0 and 1.
    query: "SELECT * FROM inventory.inventory_position\nWHERE stockout_pct_day < 0.0 OR stockout_pct_day\
      \ > 1.0\n   OR size_availability_pct < 0.0 OR size_availability_pct > 1.0"
  - name: inv_pos_rowcount
    type: sql
    dimension: accuracy
    severity: info
    description: inv_pos_rowcount
    query: SELECT COUNT(*) AS inv_pos_rows FROM inventory.inventory_position
  - name: inv_pos_stockout_rate_global
    type: sql
    dimension: accuracy
    severity: info
    description: inv_pos_stockout_rate_global
    query: 'SELECT ROUND(AVG(CASE WHEN is_stockout = TRUE THEN 1.0 ELSE 0.0 END), 4) AS global_stockout_rate

      FROM inventory.inventory_position'
