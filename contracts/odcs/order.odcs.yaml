# GENERATED by tools/generate_odcs.py — do not edit by hand.
# ODCS v3.1.0 (Bitol / Linux Foundation). OPTIONAL integration; ORDM's
# source of truth is the committed DDL / checks.sql / glossary.md.
# Regenerate: python tools/generate_odcs.py
apiVersion: v3.1.0
kind: DataContract
id: ordm-core-order
name: ordm_order
version: 1.0.0
status: draft
domain: order
tags:
- ordm
- retail
- canonical-core
description:
  purpose: ORDM canonical-core domain 'order'. Generated from the committed DDL, relationships, quality
    checks and glossary — ORDM's source of truth. ODCS is an optional integration; the model does not
    depend on it.
  usage: Optional. Consumable by contract-driven tools (e.g. DQX for data-quality execution, Lockstep
    for Unity Catalog sync, Ontos as data products). See docs/odcs-integration.md.
authoritativeDefinitions:
- url: canonical-core/order/README.md
  type: businessDefinition
- url: canonical-core/order/glossary.md
  type: businessDefinition
- url: docs/data-model-standards.md
  type: businessDefinition
schema:
- name: customer_order_line
  physicalName: orders.customer_order_line
  physicalType: table
  description: ORDM canonical-core customer-attributed order line fact. Feeds customer lifetime value
    (CLV/RFM).
  tags:
  - transactional
  authoritativeDefinitions:
  - url: canonical-core/order/README.md
    type: businessDefinition
  - url: canonical-core/order/glossary.md
    type: businessDefinition
  - url: docs/data-model-standards.md
    type: businessDefinition
  properties:
  - name: order_line_sk
    logicalType: integer
    physicalType: BIGINT
    required: true
    primaryKey: true
    primaryKeyPosition: 1
    description: Surrogate key.
  - name: order_line_id
    logicalType: string
    physicalType: STRING
    required: true
    description: Durable natural/business key for the order line.
  - name: order_id
    logicalType: string
    physicalType: STRING
    required: true
    description: Order (purchase occasion) business key; one order has several lines.
  - name: profile_sk
    logicalType: integer
    physicalType: BIGINT
    required: false
    description: FK to profile.profile_sk (version-specific).
    relationships:
    - to: profile.profile_sk
      type: foreignKey
      customProperties:
      - property: toSchema
        value: customer
      - property: enforced
        value: false
  - name: profile_id
    logicalType: string
    physicalType: STRING
    required: true
    description: Durable customer business key (pseudonymous; not direct PII).
  - name: product_sk
    logicalType: integer
    physicalType: BIGINT
    required: false
    description: FK to product.product_sk (version-specific).
    relationships:
    - to: product.product_sk
      type: foreignKey
      customProperties:
      - property: toSchema
        value: product
      - property: enforced
        value: false
  - name: product_id
    logicalType: string
    physicalType: STRING
    required: true
    description: Durable product business key.
  - name: store_sk
    logicalType: integer
    physicalType: BIGINT
    required: false
    description: FK to store.store_sk (version-specific).
    relationships:
    - to: store.store_sk
      type: foreignKey
      customProperties:
      - property: toSchema
        value: store
      - property: enforced
        value: false
  - name: store_id
    logicalType: string
    physicalType: STRING
    required: true
    description: Durable store business key.
  - name: order_date
    logicalType: date
    physicalType: DATE
    required: false
    description: Order date (calendar day). FK to fiscal_calendar.date_key (ISO 8601).
  - name: event_timestamp
    logicalType: timestamp
    physicalType: TIMESTAMP
    required: false
    description: Instant the order line was placed (UTC, ISO 8601) — event-time, distinct from load_timestamp
      (processing-time).
  - name: units
    logicalType: integer
    physicalType: INT
    required: false
    description: Units purchased on this line.
  - name: gross_amount
    logicalType: number
    physicalType: DECIMAL(18,2)
    required: false
    description: Gross line amount before discount, in the reporting/base currency.
  - name: net_amount
    logicalType: number
    physicalType: DECIMAL(18,2)
    required: false
    description: Net line amount after discount, in the reporting/base currency.
  - name: currency_code
    logicalType: string
    physicalType: STRING
    required: false
    description: Reporting/base currency of ALL monetary columns, ISO 4217 alpha-3. Equals the deploy
      base_currency; the canonical core is single-currency by construction.
  - name: transaction_currency_code
    logicalType: string
    physicalType: STRING
    required: false
    description: Original transaction currency before normalization (lineage only), ISO 4217 alpha-3.
      Convert with fx_rate.
  - name: record_source
    logicalType: string
    physicalType: STRING
    required: false
    description: Originating system of record (vendor-neutral label).
  - name: load_timestamp
    logicalType: timestamp
    physicalType: TIMESTAMP
    required: false
    description: Timestamp this row was loaded into the canonical core (UTC, ISO 8601).
  quality:
  - name: col_keys_not_null
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: col_keys_not_null
    query: "SELECT * FROM orders.customer_order_line\nWHERE order_line_sk IS NULL OR order_line_id IS\
      \ NULL OR order_id IS NULL\n   OR profile_id IS NULL OR product_id IS NULL OR store_id IS NULL OR\
      \ order_date IS NULL"
  - name: col_profile_fk_orphan
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: col_profile_fk_orphan
    query: 'SELECT col.* FROM orders.customer_order_line col

      LEFT JOIN customer.profile p ON col.profile_sk = p.profile_sk

      WHERE col.profile_sk IS NOT NULL AND p.profile_sk IS NULL'
  - name: col_product_fk_orphan
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: col_product_fk_orphan
    query: 'SELECT col.* FROM orders.customer_order_line col

      LEFT JOIN product.product pr ON col.product_sk = pr.product_sk

      WHERE col.product_sk IS NOT NULL AND pr.product_sk IS NULL'
  - name: col_store_fk_orphan
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: col_store_fk_orphan
    query: 'SELECT col.* FROM orders.customer_order_line col

      LEFT JOIN store.store st ON col.store_sk = st.store_sk

      WHERE col.store_sk IS NOT NULL AND st.store_sk IS NULL'
  - name: col_quantities_nonnegative
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: col_quantities_nonnegative
    query: 'SELECT * FROM orders.customer_order_line

      WHERE units < 0 OR gross_amount < 0'
  - name: col_calendar_coverage
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: col_calendar_coverage
    query: 'SELECT col.order_date FROM orders.customer_order_line col

      LEFT JOIN calendar.fiscal_calendar c ON col.order_date = c.date_key

      WHERE c.date_key IS NULL

      GROUP BY col.order_date'
  - name: col_single_reporting_currency
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: Amounts are normalized to the base currency; currency_code must be constant.
    query: 'SELECT COUNT(DISTINCT currency_code) AS distinct_currencies

      FROM orders.customer_order_line

      HAVING COUNT(DISTINCT currency_code) > 1'
