# GENERATED by tools/generate_odcs.py — do not edit by hand.
# ODCS v3.1.0 (Bitol / Linux Foundation). OPTIONAL integration; ORDM's
# source of truth is the committed DDL / checks.sql / glossary.md.
# Regenerate: python tools/generate_odcs.py
apiVersion: v3.1.0
kind: DataContract
id: ordm-core-supplier
name: ordm_supplier
version: 1.0.0
status: draft
domain: supplier
tags:
- ordm
- retail
- canonical-core
description:
  purpose: ORDM canonical-core domain 'supplier'. Generated from the committed DDL, relationships, quality
    checks and glossary — ORDM's source of truth. ODCS is an optional integration; the model does not
    depend on it.
  usage: Optional. Consumable by contract-driven tools (e.g. DQX for data-quality execution, Lockstep
    for Unity Catalog sync, Ontos as data products). See docs/odcs-integration.md.
authoritativeDefinitions:
- url: canonical-core/supplier/README.md
  type: businessDefinition
- url: canonical-core/supplier/glossary.md
  type: businessDefinition
- url: docs/data-model-standards.md
  type: businessDefinition
schema:
- name: supplier
  physicalName: supplier.supplier
  physicalType: table
  description: ORDM canonical-core supplier/vendor master (SCD2). Conformed supplier entity shared across
    supply-chain outcome packages.
  tags:
  - master
  authoritativeDefinitions:
  - url: canonical-core/supplier/README.md
    type: businessDefinition
  - url: canonical-core/supplier/glossary.md
    type: businessDefinition
  - url: docs/data-model-standards.md
    type: businessDefinition
  properties:
  - name: supplier_sk
    logicalType: integer
    physicalType: BIGINT
    required: true
    primaryKey: true
    primaryKeyPosition: 1
    description: Surrogate key. Unique per SCD2 version; the join/FK target.
  - name: supplier_id
    logicalType: string
    physicalType: STRING
    required: true
    description: Durable natural/business key for the supplier.
  - name: gln
    logicalType: string
    physicalType: STRING
    required: false
    description: GS1 Global Location Number (13 digits) identifying the supplier party/location.
  - name: supplier_name
    logicalType: string
    physicalType: STRING
    required: false
    description: Registered / legal name of the supplier.
  - name: supplier_type
    logicalType: string
    physicalType: STRING
    required: false
    description: 'Type of supplier. Allowed values: manufacturer, distributor, wholesaler, importer, broker.'
  - name: country_code
    logicalType: string
    physicalType: STRING
    required: false
    description: Country of the supplier, ISO 3166-1 alpha-2.
  - name: region
    logicalType: string
    physicalType: STRING
    required: false
    description: Sourcing region / area.
  - name: onboarding_date
    logicalType: date
    physicalType: DATE
    required: false
    description: Date the supplier was onboarded (ISO 8601 date).
  - name: supplier_status
    logicalType: string
    physicalType: STRING
    required: false
    description: 'Lifecycle status. Allowed values: active, inactive, suspended.'
    quality:
    - type: text
      dimension: validity
      severity: error
      description: 'Allowed values: active, inactive, suspended.'
  - name: effective_from_date
    logicalType: date
    physicalType: DATE
    required: true
    description: 'SCD2: inclusive start date this version became effective.'
  - name: effective_to_date
    logicalType: date
    physicalType: DATE
    required: false
    description: 'SCD2: exclusive end date; NULL for the current version.'
  - name: is_current
    logicalType: boolean
    physicalType: BOOLEAN
    required: true
    description: 'SCD2: TRUE for the currently active version.'
  - name: created_timestamp
    logicalType: timestamp
    physicalType: TIMESTAMP
    required: false
    description: When the record was created in the source system (UTC, ISO 8601).
  - name: source_updated_timestamp
    logicalType: timestamp
    physicalType: TIMESTAMP
    required: false
    description: When the record was last modified in the source system (UTC, ISO 8601).
  - name: record_source
    logicalType: string
    physicalType: STRING
    required: false
    description: Originating system of record (vendor-neutral label).
  - name: load_timestamp
    logicalType: timestamp
    physicalType: TIMESTAMP
    required: false
    description: When this row was loaded into the canonical core (UTC, ISO 8601).
  quality:
  - name: supplier_keys_not_null
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: supplier_keys_not_null
    query: 'SELECT * FROM supplier.supplier

      WHERE supplier_sk IS NULL OR supplier_id IS NULL OR is_current IS NULL'
  - name: supplier_business_key_unique_current
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: supplier_business_key_unique_current
    query: 'SELECT supplier_id, COUNT(*) AS current_versions

      FROM supplier.supplier

      WHERE is_current = true

      GROUP BY supplier_id

      HAVING COUNT(*) > 1'
  - name: supplier_type_domain
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: supplier_type_domain
    query: "SELECT * FROM supplier.supplier\nWHERE supplier_type IS NOT NULL\n  AND supplier_type NOT\
      \ IN ('manufacturer', 'distributor', 'wholesaler', 'importer', 'broker')"
  - name: supplier_status_domain
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: supplier_status_domain
    query: "SELECT * FROM supplier.supplier\nWHERE supplier_status IS NOT NULL\n  AND supplier_status\
      \ NOT IN ('active', 'inactive', 'suspended')"
  - name: supplier_country_code_iso
    type: sql
    dimension: accuracy
    severity: warning
    mustBe: 0
    unit: rows
    description: supplier_country_code_iso
    query: 'SELECT * FROM supplier.supplier

      WHERE country_code IS NOT NULL AND LENGTH(country_code) <> 2'
