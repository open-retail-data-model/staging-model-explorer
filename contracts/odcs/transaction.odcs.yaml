# GENERATED by tools/generate_odcs.py — do not edit by hand.
# ODCS v3.1.0 (Bitol / Linux Foundation). OPTIONAL integration; ORDM's
# source of truth is the committed DDL / checks.sql / glossary.md.
# Regenerate: python tools/generate_odcs.py
apiVersion: v3.1.0
kind: DataContract
id: ordm-core-transaction
name: ordm_transaction
version: 1.0.0
status: draft
domain: transaction
tags:
- ordm
- retail
- canonical-core
description:
  purpose: ORDM canonical-core domain 'transaction'. Generated from the committed DDL, relationships,
    quality checks and glossary — ORDM's source of truth. ODCS is an optional integration; the model does
    not depend on it.
  usage: Optional. Consumable by contract-driven tools (e.g. DQX for data-quality execution, Lockstep
    for Unity Catalog sync, Ontos as data products). See docs/odcs-integration.md.
authoritativeDefinitions:
- url: canonical-core/transaction/README.md
  type: businessDefinition
- url: canonical-core/transaction/glossary.md
  type: businessDefinition
- url: docs/data-model-standards.md
  type: businessDefinition
schema:
- name: sales
  physicalName: transaction.sales
  physicalType: table
  description: ORDM canonical-core POS sales fact (product x store x day). Carries promo_sk for promotion
    attribution.
  tags:
  - transactional
  authoritativeDefinitions:
  - url: canonical-core/transaction/README.md
    type: businessDefinition
  - url: canonical-core/transaction/glossary.md
    type: businessDefinition
  - url: docs/data-model-standards.md
    type: businessDefinition
  properties:
  - name: sales_sk
    logicalType: integer
    physicalType: BIGINT
    required: true
    primaryKey: true
    primaryKeyPosition: 1
    description: Surrogate key.
  - name: sales_id
    logicalType: string
    physicalType: STRING
    required: true
    description: Durable natural/business key for the daily sales record.
  - name: date_key
    logicalType: date
    physicalType: DATE
    required: true
    description: Selling date. FK to fiscal_calendar.date_key (ISO 8601).
  - name: product_sk
    logicalType: integer
    physicalType: BIGINT
    required: false
    description: FK to product.product_sk (version-specific).
    relationships:
    - to: product.product_sk
      type: foreignKey
      customProperties:
      - property: toSchema
        value: product
      - property: enforced
        value: false
  - name: product_id
    logicalType: string
    physicalType: STRING
    required: true
    description: Durable product business key.
  - name: store_sk
    logicalType: integer
    physicalType: BIGINT
    required: false
    description: FK to store.store_sk (version-specific).
    relationships:
    - to: store.store_sk
      type: foreignKey
      customProperties:
      - property: toSchema
        value: store
      - property: enforced
        value: false
  - name: store_id
    logicalType: string
    physicalType: STRING
    required: true
    description: Durable store business key.
  - name: promo_sk
    logicalType: integer
    physicalType: BIGINT
    required: false
    description: FK to promotion.promo_sk. Non-promoted rows carry the NO_PROMO member surrogate.
    relationships:
    - to: promotion.promo_sk
      type: foreignKey
      customProperties:
      - property: toSchema
        value: marketing
      - property: enforced
        value: false
  - name: promo_id
    logicalType: string
    physicalType: STRING
    required: false
    description: Durable promotion business key; equals NO_PROMO when the sale was not on promotion.
  - name: units
    logicalType: integer
    physicalType: INT
    required: false
    description: Units sold (count).
    quality:
    - type: text
      dimension: validity
      severity: error
      description: CHECK (units >= 0 AND gross_revenue >= 0 AND net_revenue >= 0).
  - name: gross_revenue
    logicalType: number
    physicalType: DECIMAL(18,2)
    required: false
    description: Gross sales amount before promotional discount, in the reporting/base currency.
  - name: discount_amount
    logicalType: number
    physicalType: DECIMAL(18,2)
    required: false
    description: Promotional discount amount applied, in the reporting/base currency.
  - name: net_revenue
    logicalType: number
    physicalType: DECIMAL(18,2)
    required: false
    description: Net sales amount after discount (gross_revenue - discount_amount), in the reporting/base
      currency.
  - name: currency_code
    logicalType: string
    physicalType: STRING
    required: false
    description: Reporting/base currency of ALL monetary columns, ISO 4217 alpha-3. Equals the deploy
      base_currency; the canonical core is single-currency by construction.
  - name: transaction_currency_code
    logicalType: string
    physicalType: STRING
    required: false
    description: Original transaction currency before normalization (lineage only), ISO 4217 alpha-3.
      Convert with fx_rate.
  - name: record_source
    logicalType: string
    physicalType: STRING
    required: false
    description: Originating system of record (vendor-neutral label).
  - name: load_timestamp
    logicalType: date
    physicalType: TIMESTAMP
    required: false
    description: Timestamp this row was loaded into the canonical core (UTC, ISO 8601).
  quality:
  - name: sales_keys_not_null
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: sales_keys_not_null
    query: "SELECT * FROM transaction.sales\nWHERE sales_sk IS NULL OR sales_id IS NULL OR date_key IS\
      \ NULL\n   OR product_id IS NULL OR store_id IS NULL"
  - name: sales_promo_attribution_present
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: Every sale must carry a promotion surrogate (the NO_PROMO member when not promoted).
    query: 'SELECT * FROM transaction.sales

      WHERE promo_sk IS NULL OR promo_id IS NULL'
  - name: sales_promotion_fk_orphan
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: sales_promotion_fk_orphan
    query: 'SELECT s.* FROM transaction.sales s

      LEFT JOIN marketing.promotion p ON s.promo_sk = p.promo_sk

      WHERE s.promo_sk IS NOT NULL AND p.promo_sk IS NULL'
  - name: sales_product_fk_orphan
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: sales_product_fk_orphan
    query: 'SELECT s.* FROM transaction.sales s

      LEFT JOIN product.product p ON s.product_sk = p.product_sk

      WHERE s.product_sk IS NOT NULL AND p.product_sk IS NULL'
  - name: sales_store_fk_orphan
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: sales_store_fk_orphan
    query: 'SELECT s.* FROM transaction.sales s

      LEFT JOIN store.store st ON s.store_sk = st.store_sk

      WHERE s.store_sk IS NOT NULL AND st.store_sk IS NULL'
  - name: sales_nonnegative_measures
    type: sql
    dimension: accuracy
    severity: warning
    mustBe: 0
    unit: rows
    description: sales_nonnegative_measures
    query: 'SELECT * FROM transaction.sales

      WHERE units < 0 OR gross_revenue < 0 OR net_revenue < 0'
  - name: sales_calendar_coverage
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: Every selling date must exist in the fiscal calendar (required for week rollups).
    query: 'SELECT s.date_key FROM transaction.sales s

      LEFT JOIN calendar.fiscal_calendar c ON s.date_key = c.date_key

      WHERE c.date_key IS NULL

      GROUP BY s.date_key'
  - name: sales_single_reporting_currency
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: Monetary columns are normalized to the base currency, so currency_code must be constant
      across the fact (gold SUMs are then single-currency by construction).
    query: 'SELECT COUNT(DISTINCT currency_code) AS distinct_currencies

      FROM transaction.sales

      HAVING COUNT(DISTINCT currency_code) > 1'
  - name: sales_fk_points_to_current_version
    type: sql
    dimension: accuracy
    severity: info
    description: 'Informational: share (%) of sales rows whose product_sk / store_sk resolve to a NON-current
      dimension version. Historical facts may legitimately reference prior SCD2 versions, so this is a
      metric (never fails) that documents the expectation.'
    query: "SELECT ROUND(100.0 * SUM(\n         CASE WHEN COALESCE(p.is_current, true) AND COALESCE(st.is_current,\
      \ true)\n              THEN 0 ELSE 1 END) / COUNT(*), 2) AS pct_sales_noncurrent_version\nFROM transaction.sales\
      \ s\nLEFT JOIN product.product p ON s.product_sk = p.product_sk\nLEFT JOIN store.store st ON s.store_sk\
      \ = st.store_sk"
