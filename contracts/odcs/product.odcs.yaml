# GENERATED by tools/generate_odcs.py — do not edit by hand.
# ODCS v3.1.0 (Bitol / Linux Foundation). OPTIONAL integration; ORDM's
# source of truth is the committed DDL / checks.sql / glossary.md.
# Regenerate: python tools/generate_odcs.py
apiVersion: v3.1.0
kind: DataContract
id: ordm-core-product
name: ordm_product
version: 1.0.0
status: draft
domain: product
tags:
- ordm
- retail
- canonical-core
description:
  purpose: ORDM canonical-core domain 'product'. Generated from the committed DDL, relationships, quality
    checks and glossary — ORDM's source of truth. ODCS is an optional integration; the model does not
    depend on it.
  usage: Optional. Consumable by contract-driven tools (e.g. DQX for data-quality execution, Lockstep
    for Unity Catalog sync, Ontos as data products). See docs/odcs-integration.md.
authoritativeDefinitions:
- url: canonical-core/product/README.md
  type: businessDefinition
- url: canonical-core/product/glossary.md
  type: businessDefinition
- url: docs/data-model-standards.md
  type: businessDefinition
schema:
- name: product
  physicalName: product.product
  physicalType: table
  description: ORDM canonical-core product master (SCD2). Conformed product entity shared across outcome
    packages.
  tags:
  - master
  authoritativeDefinitions:
  - url: canonical-core/product/README.md
    type: businessDefinition
  - url: canonical-core/product/glossary.md
    type: businessDefinition
  - url: docs/data-model-standards.md
    type: businessDefinition
  properties:
  - name: product_sk
    logicalType: integer
    physicalType: BIGINT
    required: true
    primaryKey: true
    primaryKeyPosition: 1
    description: Surrogate key. Unique per SCD2 version; the join/FK target.
  - name: product_id
    logicalType: string
    physicalType: STRING
    required: true
    description: Durable natural/business key for the product.
  - name: gtin
    logicalType: string
    physicalType: STRING
    required: false
    description: GS1 Global Trade Item Number (UPC/EAN). Not PII.
  - name: sku
    logicalType: string
    physicalType: STRING
    required: false
    description: Stock keeping unit code. Not PII.
  - name: style_id
    logicalType: string
    physicalType: STRING
    required: false
    description: Parent style/model this SKU is a variant of. SKUs sharing a style_id are variants (e.g.
      colour/size). NULL for non-variant products.
  - name: color
    logicalType: string
    physicalType: STRING
    required: false
    description: 'Variant-defining attribute: colour (free text). NULL if not applicable.'
  - name: size
    logicalType: string
    physicalType: STRING
    required: false
    description: 'Variant-defining attribute: size (free text, e.g. S/M/L, 500ml). NULL if not applicable.'
  - name: product_name
    logicalType: string
    physicalType: STRING
    required: false
    description: Display name of the product.
  - name: brand
    logicalType: string
    physicalType: STRING
    required: false
    description: Brand name (free text).
  - name: category
    logicalType: string
    physicalType: STRING
    required: false
    description: Merchandising category (top level).
  - name: subcategory
    logicalType: string
    physicalType: STRING
    required: false
    description: Merchandising subcategory.
  - name: department
    logicalType: string
    physicalType: STRING
    required: false
    description: Store department the product belongs to.
  - name: unit_of_measure
    logicalType: string
    physicalType: STRING
    required: false
    description: 'Selling unit of measure. Allowed values: each, kg, g, l, ml, pack.'
    quality:
    - type: text
      dimension: validity
      severity: error
      description: 'Allowed values: each, kg, g, l, ml, pack.'
  - name: list_price
    logicalType: number
    physicalType: DECIMAL(18,4)
    required: false
    description: 'Standard list (shelf) price, in the reporting/base currency (normalized at ingest).
      Unit-grain price: DECIMAL(18,4) (principle #9d).'
  - name: unit_cost
    logicalType: number
    physicalType: DECIMAL(18,4)
    required: false
    description: 'Standard unit cost (COGS), in the reporting/base currency. Basis for margin = revenue
      - units * unit_cost. A master cost attribute, not a derived metric. Unit-grain cost: DECIMAL(18,4)
      (principle #9d).'
  - name: currency_code
    logicalType: string
    physicalType: STRING
    required: false
    description: Reporting/base currency of ALL monetary columns, ISO 4217 alpha-3. Equals the deploy
      base_currency; the canonical core is single-currency by construction.
  - name: transaction_currency_code
    logicalType: string
    physicalType: STRING
    required: false
    description: Original sourcing currency before normalization (lineage only), ISO 4217 alpha-3. Convert
      with fx_rate.
  - name: product_status
    logicalType: string
    physicalType: STRING
    required: false
    description: 'Lifecycle status. Allowed values: active, inactive, discontinued.'
    quality:
    - type: text
      dimension: validity
      severity: error
      description: 'Allowed values: active, inactive, discontinued.'
  - name: effective_from_date
    logicalType: date
    physicalType: DATE
    required: true
    description: 'SCD2: inclusive start date this version became effective.'
  - name: effective_to_date
    logicalType: date
    physicalType: DATE
    required: false
    description: 'SCD2: exclusive end date; NULL for the current version.'
  - name: is_current
    logicalType: boolean
    physicalType: BOOLEAN
    required: true
    description: 'SCD2: TRUE for the currently active version.'
  - name: created_timestamp
    logicalType: date
    physicalType: TIMESTAMP
    required: false
    description: When the record was created in the source system (UTC, ISO 8601).
  - name: source_updated_timestamp
    logicalType: date
    physicalType: TIMESTAMP
    required: false
    description: When the record was last modified in the source system (UTC, ISO 8601).
  - name: record_source
    logicalType: string
    physicalType: STRING
    required: false
    description: Originating system of record (vendor-neutral label).
  - name: load_timestamp
    logicalType: date
    physicalType: TIMESTAMP
    required: false
    description: When this row was loaded into the canonical core (UTC, ISO 8601).
  quality:
  - name: product_keys_not_null
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: product_keys_not_null
    query: 'SELECT * FROM product.product

      WHERE product_sk IS NULL OR product_id IS NULL OR is_current IS NULL'
  - name: product_business_key_unique_current
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: product_business_key_unique_current
    query: 'SELECT product_id, COUNT(*) AS current_versions

      FROM product.product

      WHERE is_current = true

      GROUP BY product_id

      HAVING COUNT(*) > 1'
  - name: product_status_domain
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: product_status_domain
    query: "SELECT * FROM product.product\nWHERE product_status IS NOT NULL\n  AND product_status NOT\
      \ IN ('active', 'inactive', 'discontinued')"
  - name: product_uom_domain
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: product_uom_domain
    query: "SELECT * FROM product.product\nWHERE unit_of_measure IS NOT NULL\n  AND unit_of_measure NOT\
      \ IN ('each', 'kg', 'g', 'l', 'ml', 'pack')"
  - name: product_unit_cost_nonnegative
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: product_unit_cost_nonnegative
    query: 'SELECT * FROM product.product

      WHERE unit_cost IS NOT NULL AND unit_cost < 0'
  - name: product_unit_cost_below_list_price
    type: sql
    dimension: accuracy
    severity: warning
    mustBe: 0
    unit: rows
    description: Cost above list price means a structural loss-leader; surface it for review.
    query: 'SELECT * FROM product.product

      WHERE unit_cost IS NOT NULL AND list_price IS NOT NULL AND unit_cost > list_price'
  - name: product_single_reporting_currency
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: 'The canonical core is single-currency: every monetary column is stored in the base currency,
      so currency_code must be constant. (transaction_currency_code carries the original sourcing currency
      and is intentionally NOT constrained.)'
    query: 'SELECT COUNT(DISTINCT currency_code) AS distinct_currencies

      FROM product.product

      HAVING COUNT(DISTINCT currency_code) > 1'
- name: product_price
  physicalName: product.product_price
  physicalType: table
  description: ORDM canonical-core product price history (SCD2 per product x price_type). Read price AS
    OF a date here; product dim carries the current snapshot.
  tags:
  - reference
  authoritativeDefinitions:
  - url: canonical-core/product/README.md
    type: businessDefinition
  - url: canonical-core/product/glossary.md
    type: businessDefinition
  - url: docs/data-model-standards.md
    type: businessDefinition
  properties:
  - name: product_price_sk
    logicalType: integer
    physicalType: BIGINT
    required: true
    primaryKey: true
    primaryKeyPosition: 1
    description: Surrogate key. Unique per price version.
  - name: product_price_id
    logicalType: string
    physicalType: STRING
    required: true
    description: Durable business key (product_id|price_type|effective_from).
  - name: product_sk
    logicalType: integer
    physicalType: BIGINT
    required: false
    description: FK to product.product_sk (foreign surrogate; nullable, not a PK). Resolved AS OF effective_from_date
      — the product SCD2 version in effect when this price applied — matching how sales/inventory/shipment
      resolve their _sk FKs. Declared as informational FK fk_product_price_product in relationships.sql
      (the durable product_id business key is retained below).
    relationships:
    - to: product.product_sk
      type: foreignKey
      customProperties:
      - property: toSchema
        value: product
      - property: enforced
        value: false
  - name: product_id
    logicalType: string
    physicalType: STRING
    required: true
    description: Durable product business key. The priced product. Retained alongside product_sk; the
      declared UC FK is on product_sk (under SCD2 the business key is not unique, so the FK must be on
      the surrogate).
  - name: price_type
    logicalType: string
    physicalType: STRING
    required: false
    description: 'What the amount represents. Allowed values: list, cost, promotional, contract.'
    quality:
    - type: text
      dimension: validity
      severity: error
      description: 'Allowed values: list, cost, promotional, contract.'
  - name: amount
    logicalType: number
    physicalType: DECIMAL(18,4)
    required: false
    description: 'Unit-grain price/cost (principle #9d), in the reporting/base currency.'
    quality:
    - type: text
      dimension: validity
      severity: error
      description: CHECK (amount IS NULL OR amount >= 0).
  - name: currency_code
    logicalType: string
    physicalType: STRING
    required: false
    description: Reporting/base currency, ISO 4217 alpha-3. Equals the deploy base_currency; single-currency
      by construction.
  - name: transaction_currency_code
    logicalType: string
    physicalType: STRING
    required: false
    description: Original currency before normalization (lineage only), ISO 4217 alpha-3. Convert with
      fx_rate.
  - name: effective_from_date
    logicalType: date
    physicalType: DATE
    required: true
    description: 'SCD2: inclusive start date this price became effective.'
  - name: effective_to_date
    logicalType: date
    physicalType: DATE
    required: false
    description: 'SCD2: exclusive end date; NULL for the current price.'
  - name: is_current
    logicalType: boolean
    physicalType: BOOLEAN
    required: true
    description: 'SCD2: TRUE for the currently effective price per (product, price_type).'
  - name: created_timestamp
    logicalType: date
    physicalType: TIMESTAMP
    required: false
    description: When the record was created in the source system (UTC, ISO 8601).
  - name: source_updated_timestamp
    logicalType: date
    physicalType: TIMESTAMP
    required: false
    description: When the record was last modified in the source system (UTC, ISO 8601).
  - name: record_source
    logicalType: string
    physicalType: STRING
    required: false
    description: Originating system of record (vendor-neutral label).
  - name: load_timestamp
    logicalType: date
    physicalType: TIMESTAMP
    required: false
    description: When this row was loaded into the canonical core (UTC, ISO 8601).
  quality:
  - name: product_price_keys_not_null
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: product_price_keys_not_null
    query: "SELECT * FROM product.product_price\nWHERE product_price_sk IS NULL OR product_price_id IS\
      \ NULL OR product_id IS NULL\n   OR price_type IS NULL OR effective_from_date IS NULL OR is_current\
      \ IS NULL"
  - name: product_price_type_domain
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: product_price_type_domain
    query: 'SELECT * FROM product.product_price

      WHERE price_type NOT IN (''list'', ''cost'', ''promotional'', ''contract'')'
  - name: product_price_amount_nonnegative
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: product_price_amount_nonnegative
    query: 'SELECT * FROM product.product_price

      WHERE amount IS NOT NULL AND amount < 0'
  - name: product_price_single_current_per_type
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: At most one current price per (product, price_type).
    query: 'SELECT product_id, price_type, COUNT(*) AS current_rows

      FROM product.product_price

      WHERE is_current = true

      GROUP BY product_id, price_type

      HAVING COUNT(*) > 1'
  - name: product_price_product_orphan
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: Every priced product_id must exist in the product master.
    query: "SELECT pp.product_id FROM product.product_price pp\nLEFT JOIN (SELECT DISTINCT product_id\
      \ FROM product.product) p\n  ON pp.product_id = p.product_id\nWHERE p.product_id IS NULL\nGROUP\
      \ BY pp.product_id"
  - name: product_price_product_sk_orphan
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: 'The declared FK is on the surrogate: every non-null product_sk must exist in the product
      master.'
    query: 'SELECT pp.* FROM product.product_price pp

      LEFT JOIN product.product p ON pp.product_sk = p.product_sk

      WHERE pp.product_sk IS NOT NULL AND p.product_sk IS NULL'
  - name: product_price_single_reporting_currency
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: product_price_single_reporting_currency
    query: 'SELECT COUNT(DISTINCT currency_code) AS distinct_currencies

      FROM product.product_price

      HAVING COUNT(DISTINCT currency_code) > 1'
