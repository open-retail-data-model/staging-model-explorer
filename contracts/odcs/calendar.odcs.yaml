# GENERATED by tools/generate_odcs.py — do not edit by hand.
# ODCS v3.1.0 (Bitol / Linux Foundation). OPTIONAL integration; ORDM's
# source of truth is the committed DDL / checks.sql / glossary.md.
# Regenerate: python tools/generate_odcs.py
apiVersion: v3.1.0
kind: DataContract
id: ordm-core-calendar
name: ordm_calendar
version: 1.0.0
status: draft
domain: calendar
tags:
- ordm
- retail
- canonical-core
description:
  purpose: ORDM canonical-core domain 'calendar'. Generated from the committed DDL, relationships, quality
    checks and glossary — ORDM's source of truth. ODCS is an optional integration; the model does not
    depend on it.
  usage: Optional. Consumable by contract-driven tools (e.g. DQX for data-quality execution, Lockstep
    for Unity Catalog sync, Ontos as data products). See docs/odcs-integration.md.
authoritativeDefinitions:
- url: canonical-core/calendar/README.md
  type: businessDefinition
- url: canonical-core/calendar/glossary.md
  type: businessDefinition
- url: docs/data-model-standards.md
  type: businessDefinition
schema:
- name: fiscal_calendar
  physicalName: calendar.fiscal_calendar
  physicalType: table
  description: ORDM canonical-core NRF 4-5-4 retail fiscal calendar (day grain). Conformed time dimension.
  tags:
  - reference
  authoritativeDefinitions:
  - url: canonical-core/calendar/README.md
    type: businessDefinition
  - url: canonical-core/calendar/glossary.md
    type: businessDefinition
  - url: docs/data-model-standards.md
    type: businessDefinition
  properties:
  - name: date_sk
    logicalType: integer
    physicalType: BIGINT
    required: true
    primaryKey: true
    primaryKeyPosition: 1
    description: Surrogate key.
  - name: date_key
    logicalType: date
    physicalType: DATE
    required: true
    description: The calendar date (ISO 8601). Natural key; join target for the sales fact.
  - name: date_id
    logicalType: string
    physicalType: STRING
    required: true
    description: 'Durable STRING business key: the date rendered ISO 8601 (yyyy-MM-dd). Version-independent,
      stable across reloads; the _id counterpart to the surrogate date_sk.'
  - name: calendar_year
    logicalType: integer
    physicalType: INT
    required: false
    description: Gregorian calendar year.
  - name: calendar_month
    logicalType: integer
    physicalType: INT
    required: false
    description: Gregorian month (1-12).
    quality:
    - type: text
      dimension: validity
      severity: error
      description: CHECK (calendar_month IS NULL OR calendar_month BETWEEN 1 AND 12).
  - name: calendar_day
    logicalType: integer
    physicalType: INT
    required: false
    description: Day of month (1-31).
    quality:
    - type: text
      dimension: validity
      severity: error
      description: CHECK (calendar_day IS NULL OR calendar_day BETWEEN 1 AND 31).
  - name: day_of_week
    logicalType: integer
    physicalType: INT
    required: false
    description: ISO day of week (1=Monday .. 7=Sunday).
    quality:
    - type: text
      dimension: validity
      severity: error
      description: CHECK (day_of_week IS NULL OR day_of_week BETWEEN 1 AND 7).
  - name: day_name
    logicalType: string
    physicalType: STRING
    required: false
    description: Day name (monday .. sunday).
    quality:
    - type: text
      dimension: validity
      severity: error
      description: 'Allowed values: monday, tuesday, wednesday, thursday, friday, saturday, sunday.'
  - name: is_weekend
    logicalType: boolean
    physicalType: BOOLEAN
    required: false
    description: TRUE for Saturday/Sunday.
  - name: is_holiday
    logicalType: boolean
    physicalType: BOOLEAN
    required: false
    description: TRUE if a recognized retail holiday.
  - name: fiscal_year
    logicalType: integer
    physicalType: INT
    required: false
    description: NRF 4-5-4 fiscal year.
  - name: fiscal_quarter
    logicalType: integer
    physicalType: INT
    required: false
    description: NRF fiscal quarter (1-4).
    quality:
    - type: text
      dimension: validity
      severity: error
      description: CHECK (fiscal_quarter IS NULL OR fiscal_quarter BETWEEN 1 AND 4).
  - name: fiscal_period
    logicalType: integer
    physicalType: INT
    required: false
    description: NRF fiscal period / retail month (1-12), following the 4-5-4 week pattern.
    quality:
    - type: text
      dimension: validity
      severity: error
      description: CHECK (fiscal_period IS NULL OR fiscal_period BETWEEN 1 AND 12).
  - name: fiscal_season
    logicalType: string
    physicalType: STRING
    required: false
    description: 'Retail merchandising half-year season derived from fiscal_period: spring for periods
      1-6 (fiscal H1), fall for periods 7-12 (fiscal H2). Labels follow the northern-hemisphere retail-season
      convention; they name the fiscal half, not a local meteorological season. Allowed values: spring,
      fall.'
    quality:
    - type: text
      dimension: validity
      severity: error
      description: 'Allowed values: spring, fall.'
  - name: fiscal_week
    logicalType: integer
    physicalType: INT
    required: false
    description: NRF fiscal week within the fiscal year (1-53).
    quality:
    - type: text
      dimension: validity
      severity: error
      description: CHECK (fiscal_week IS NULL OR fiscal_week BETWEEN 1 AND 53).
  - name: fiscal_week_id
    logicalType: integer
    physicalType: INT
    required: false
    description: Week identifier = fiscal_year * 100 + fiscal_week (e.g. 202614). Week-level join key.
  - name: fiscal_week_index
    logicalType: integer
    physicalType: INT
    required: false
    description: Globally monotonic week counter across the whole calendar; use for trailing N-week windows.
  - name: fiscal_week_start_date
    logicalType: date
    physicalType: DATE
    required: false
    description: First date (Sunday, NRF convention) of the fiscal week.
  - name: fiscal_week_end_date
    logicalType: date
    physicalType: DATE
    required: false
    description: Last date (Saturday) of the fiscal week.
  - name: created_timestamp
    logicalType: timestamp
    physicalType: TIMESTAMP
    required: false
    description: When the record was created in the source system (UTC, ISO 8601). Required on reference
      tables (standard §8).
  - name: source_updated_timestamp
    logicalType: timestamp
    physicalType: TIMESTAMP
    required: false
    description: When the record was last modified in the source system (UTC, ISO 8601). Required on reference
      tables (standard §8).
  - name: record_source
    logicalType: string
    physicalType: STRING
    required: false
    description: Originating system of record (vendor-neutral label).
  - name: load_timestamp
    logicalType: timestamp
    physicalType: TIMESTAMP
    required: false
    description: Timestamp this row was loaded into the canonical core (UTC, ISO 8601).
  quality:
  - name: calendar_keys_not_null
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: calendar_keys_not_null
    query: "SELECT * FROM calendar.fiscal_calendar\nWHERE date_sk IS NULL OR date_key IS NULL OR date_id\
      \ IS NULL\n   OR fiscal_week_id IS NULL OR fiscal_week_index IS NULL"
  - name: calendar_date_unique
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: calendar_date_unique
    query: 'SELECT date_key, COUNT(*) AS rows

      FROM calendar.fiscal_calendar

      GROUP BY date_key

      HAVING COUNT(*) > 1'
  - name: calendar_fiscal_ranges
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: 'NRF 4-5-4 bounds: week 1-53, quarter 1-4, period 1-12.'
    query: "SELECT * FROM calendar.fiscal_calendar\nWHERE fiscal_week NOT BETWEEN 1 AND 53\n   OR fiscal_quarter\
      \ NOT BETWEEN 1 AND 4\n   OR fiscal_period NOT BETWEEN 1 AND 12"
  - name: calendar_fiscal_season_matches_period
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: 'fiscal_season is derived from fiscal_period: spring = periods 1-6 (H1), fall = 7-12
      (H2). Catches a season that disagrees with its period (derivation drift), plus any value outside
      the two-season domain.'
    query: "SELECT * FROM calendar.fiscal_calendar\nWHERE fiscal_season IS NOT NULL\n  AND ( fiscal_season\
      \ NOT IN ('spring', 'fall')\n     OR (fiscal_season = 'spring' AND fiscal_period NOT BETWEEN 1 AND\
      \ 6)\n     OR (fiscal_season = 'fall'   AND fiscal_period NOT BETWEEN 7 AND 12) )"
  - name: calendar_week_date_order
    type: sql
    dimension: accuracy
    severity: warning
    mustBe: 0
    unit: rows
    description: calendar_week_date_order
    query: 'SELECT * FROM calendar.fiscal_calendar

      WHERE fiscal_week_end_date < fiscal_week_start_date'
  - name: calendar_weeks_per_year_4_5_4
    type: sql
    dimension: accuracy
    severity: warning
    mustBe: 0
    unit: rows
    description: 'NRF 4-5-4 allows 52 or 53 weeks per fiscal year. This flags any fiscal year with MORE
      than 53 weeks (a calendar bug). Partial leading/trailing years in a bounded synthetic span are expected
      and not flagged. NOTE: the synthetic generator models 52-week years only (no NRF 53rd leap week)
      — see synthetic-data/generators/trade_promotion.py.'
    query: 'SELECT fiscal_year, COUNT(DISTINCT fiscal_week) AS weeks_in_year

      FROM calendar.fiscal_calendar

      GROUP BY fiscal_year

      HAVING COUNT(DISTINCT fiscal_week) > 53'
- name: fx_rate
  physicalName: calendar.fx_rate
  physicalType: table
  description: ORDM canonical-core daily FX rates (from -> base). Conversion reference for single-currency
    normalization.
  tags:
  - reference
  authoritativeDefinitions:
  - url: canonical-core/calendar/README.md
    type: businessDefinition
  - url: canonical-core/calendar/glossary.md
    type: businessDefinition
  - url: docs/data-model-standards.md
    type: businessDefinition
  properties:
  - name: fx_rate_sk
    logicalType: integer
    physicalType: BIGINT
    required: true
    primaryKey: true
    primaryKeyPosition: 1
    description: Surrogate key.
  - name: fx_rate_id
    logicalType: string
    physicalType: STRING
    required: true
    description: Durable business key (date_key|from|to).
  - name: date_key
    logicalType: date
    physicalType: DATE
    required: true
    description: Rate date. Logical join key to fiscal_calendar.date_key (not a declared UC FK; ISO 8601).
  - name: from_currency_code
    logicalType: string
    physicalType: STRING
    required: true
    description: Source currency, ISO 4217 alpha-3.
    quality:
    - type: text
      dimension: validity
      severity: error
      description: CHECK (from_currency_code IS NULL OR char_length(from_currency_code) = 3).
  - name: to_currency_code
    logicalType: string
    physicalType: STRING
    required: true
    description: Target/reporting currency, ISO 4217 alpha-3 (the base_currency).
    quality:
    - type: text
      dimension: validity
      severity: error
      description: CHECK (to_currency_code IS NULL OR char_length(to_currency_code) = 3).
  - name: rate
    logicalType: number
    physicalType: DECIMAL(18,8)
    required: false
    description: Multiply a from-currency amount by rate to get the to-currency amount. 1.0 when from
      = to.
    quality:
    - type: text
      dimension: validity
      severity: error
      description: CHECK (rate IS NULL OR rate > 0).
  - name: created_timestamp
    logicalType: timestamp
    physicalType: TIMESTAMP
    required: false
    description: When the record was created in the source system (UTC, ISO 8601). Required on reference
      tables (standard §8).
  - name: source_updated_timestamp
    logicalType: timestamp
    physicalType: TIMESTAMP
    required: false
    description: When the record was last modified in the source system (UTC, ISO 8601). Required on reference
      tables (standard §8).
  - name: record_source
    logicalType: string
    physicalType: STRING
    required: false
    description: Originating system of record (vendor-neutral label).
  - name: load_timestamp
    logicalType: timestamp
    physicalType: TIMESTAMP
    required: false
    description: Timestamp this row was loaded into the canonical core (UTC, ISO 8601).
  quality:
  - name: fx_rate_keys_not_null
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: fx_rate_keys_not_null
    query: "SELECT * FROM calendar.fx_rate\nWHERE fx_rate_sk IS NULL OR fx_rate_id IS NULL OR date_key\
      \ IS NULL\n   OR from_currency_code IS NULL OR to_currency_code IS NULL OR rate IS NULL"
  - name: fx_rate_id_unique
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: One rate per (date, from, to).
    query: 'SELECT date_key, from_currency_code, to_currency_code, COUNT(*) AS rows

      FROM calendar.fx_rate

      GROUP BY date_key, from_currency_code, to_currency_code

      HAVING COUNT(*) > 1'
  - name: fx_rate_positive
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: FX rates must be strictly positive.
    query: 'SELECT * FROM calendar.fx_rate

      WHERE rate <= 0'
  - name: fx_rate_base_self_rate
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: The base->base self-rate must be exactly 1 (identity conversion).
    query: 'SELECT * FROM calendar.fx_rate

      WHERE from_currency_code = to_currency_code AND rate <> 1'
  - name: fx_rate_calendar_coverage
    type: sql
    dimension: accuracy
    severity: warning
    mustBe: 0
    unit: rows
    description: Every rate date should exist in the fiscal calendar.
    query: 'SELECT fx.date_key FROM calendar.fx_rate fx

      LEFT JOIN calendar.fiscal_calendar c ON fx.date_key = c.date_key

      WHERE c.date_key IS NULL

      GROUP BY fx.date_key'
  - name: fx_rate_single_base
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: 'The canonical core normalizes to ONE reporting currency: every row must convert TO the
      same base currency.'
    query: 'SELECT COUNT(DISTINCT to_currency_code) AS distinct_base

      FROM calendar.fx_rate

      HAVING COUNT(DISTINCT to_currency_code) > 1'
