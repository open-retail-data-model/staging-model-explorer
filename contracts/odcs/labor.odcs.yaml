# GENERATED by tools/generate_odcs.py — do not edit by hand.
# ODCS v3.1.0 (Bitol / Linux Foundation). OPTIONAL integration; ORDM's
# source of truth is the committed DDL / checks.sql / glossary.md.
# Regenerate: python tools/generate_odcs.py
apiVersion: v3.1.0
kind: DataContract
id: ordm-core-labor
name: ordm_labor
version: 1.0.0
status: draft
domain: labor
tags:
- ordm
- retail
- canonical-core
description:
  purpose: ORDM canonical-core domain 'labor'. Generated from the committed DDL, relationships, quality
    checks and glossary — ORDM's source of truth. ODCS is an optional integration; the model does not
    depend on it.
  usage: Optional. Consumable by contract-driven tools (e.g. DQX for data-quality execution, Lockstep
    for Unity Catalog sync, Ontos as data products). See docs/odcs-integration.md.
authoritativeDefinitions:
- url: canonical-core/labor/README.md
  type: businessDefinition
- url: canonical-core/labor/glossary.md
  type: businessDefinition
- url: docs/data-model-standards.md
  type: businessDefinition
schema:
- name: employee
  physicalName: labor.employee
  physicalType: table
  description: ORDM canonical-core pseudonymous associate master (SCD2). Workforce supply attributes for
    scheduling; no personal names or contact PII.
  tags:
  - master
  authoritativeDefinitions:
  - url: canonical-core/labor/README.md
    type: businessDefinition
  - url: canonical-core/labor/glossary.md
    type: businessDefinition
  - url: docs/data-model-standards.md
    type: businessDefinition
  properties:
  - name: employee_sk
    logicalType: integer
    physicalType: BIGINT
    required: true
    primaryKey: true
    primaryKeyPosition: 1
    description: Surrogate key. Unique per SCD2 version; the join/FK target.
  - name: employee_id
    logicalType: string
    physicalType: STRING
    required: true
    description: Durable pseudonymous business key for the associate. Not a legal name or government ID.
    classification: restricted
    tags:
    - dbx_pii_identifier
  - name: home_store_sk
    logicalType: integer
    physicalType: BIGINT
    required: false
    description: FK to store.store_sk for the associate home store (version-specific).
    relationships:
    - to: store.store_sk
      type: foreignKey
      customProperties:
      - property: toSchema
        value: store
      - property: enforced
        value: false
  - name: home_store_id
    logicalType: string
    physicalType: STRING
    required: true
    description: Durable business key of the home store.
  - name: primary_role_code
    logicalType: string
    physicalType: STRING
    required: true
    description: 'Primary role. Allowed values: cashier, sales_associate, fulfillment, inventory, supervisor.'
  - name: employment_status
    logicalType: string
    physicalType: STRING
    required: true
    description: 'Employment status. Allowed values: active, leave, terminated.'
  - name: employment_type
    logicalType: string
    physicalType: STRING
    required: false
    description: 'Engagement type. Allowed values: full_time, part_time, seasonal, contractor.'
  - name: hourly_labor_cost
    logicalType: number
    physicalType: DECIMAL(18,4)
    required: false
    description: Fully-loaded hourly labor cost in base currency (unit-grain rate; DECIMAL scale 4).
  - name: currency_code
    logicalType: string
    physicalType: STRING
    required: false
    description: ISO 4217 alpha-3 for hourly_labor_cost; constant base_currency (USD) per single-reporting-currency
      rule.
  - name: max_daily_hours
    logicalType: number
    physicalType: DECIMAL(6,2)
    required: true
    description: Maximum scheduled hours in a single calendar day.
  - name: max_weekly_hours
    logicalType: number
    physicalType: DECIMAL(6,2)
    required: true
    description: Maximum scheduled hours in a calendar week (Mon-Sun UTC).
  - name: min_rest_hours
    logicalType: number
    physicalType: DECIMAL(6,2)
    required: true
    description: Minimum rest hours required between the end of one shift and the start of the next.
  - name: preferred_shift_start_local
    logicalType: string
    physicalType: STRING
    required: false
    description: Preferred local shift start as HH:mm (store-local); advisory only.
  - name: preferred_shift_end_local
    logicalType: string
    physicalType: STRING
    required: false
    description: Preferred local shift end as HH:mm (store-local); advisory only.
  - name: hire_date
    logicalType: date
    physicalType: DATE
    required: false
    description: Date employment began (ISO 8601).
  - name: termination_date
    logicalType: date
    physicalType: DATE
    required: false
    description: Date employment ended; NULL while active (ISO 8601).
  - name: effective_from_date
    logicalType: date
    physicalType: DATE
    required: true
    description: 'SCD2: inclusive start date this version became effective.'
  - name: effective_to_date
    logicalType: date
    physicalType: DATE
    required: false
    description: 'SCD2: exclusive end date; NULL for the current version.'
  - name: is_current
    logicalType: boolean
    physicalType: BOOLEAN
    required: true
    description: 'SCD2: TRUE for the currently active version.'
  - name: created_timestamp
    logicalType: timestamp
    physicalType: TIMESTAMP
    required: false
    description: When the record was created in the source system (UTC, ISO 8601).
  - name: source_updated_timestamp
    logicalType: timestamp
    physicalType: TIMESTAMP
    required: false
    description: When the record was last modified in the source system (UTC, ISO 8601).
  - name: record_source
    logicalType: string
    physicalType: STRING
    required: false
    description: Originating system of record (vendor-neutral label).
  - name: load_timestamp
    logicalType: timestamp
    physicalType: TIMESTAMP
    required: false
    description: When this row was loaded into the canonical core (UTC, ISO 8601).
  quality:
  - name: employee_keys_not_null
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: employee_keys_not_null
    query: "SELECT * FROM labor.employee\nWHERE employee_sk IS NULL OR employee_id IS NULL OR home_store_id\
      \ IS NULL\n   OR primary_role_code IS NULL OR employment_status IS NULL\n   OR max_daily_hours IS\
      \ NULL OR max_weekly_hours IS NULL OR min_rest_hours IS NULL\n   OR effective_from_date IS NULL\
      \ OR is_current IS NULL"
  - name: employee_pk_unique
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: employee_pk_unique
    query: 'SELECT employee_sk

      FROM labor.employee

      GROUP BY employee_sk

      HAVING COUNT(*) > 1'
  - name: employee_scd2_current_open
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: employee_scd2_current_open
    query: 'SELECT * FROM labor.employee

      WHERE is_current = TRUE AND effective_to_date IS NOT NULL'
  - name: employee_scd2_noncurrent_closed
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: employee_scd2_noncurrent_closed
    query: 'SELECT * FROM labor.employee

      WHERE is_current = FALSE AND effective_to_date IS NULL'
  - name: employee_scd2_start_before_end
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: employee_scd2_start_before_end
    query: 'SELECT * FROM labor.employee

      WHERE effective_to_date IS NOT NULL AND effective_from_date > effective_to_date'
  - name: employee_current_id_unique
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: employee_current_id_unique
    query: 'SELECT employee_id, COUNT(*) AS n

      FROM labor.employee

      WHERE is_current = TRUE

      GROUP BY employee_id

      HAVING COUNT(*) > 1'
  - name: employee_store_fk_orphan
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: employee_store_fk_orphan
    query: 'SELECT e.* FROM labor.employee e

      LEFT JOIN store.store st ON e.home_store_sk = st.store_sk

      WHERE st.store_sk IS NULL AND e.home_store_sk IS NOT NULL'
  - name: employee_cost_single_currency
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: employee_cost_single_currency
    query: 'SELECT ''employee'' AS table_name, COUNT(DISTINCT currency_code) AS currencies

      FROM labor.employee

      WHERE currency_code IS NOT NULL AND hourly_labor_cost IS NOT NULL

      HAVING COUNT(DISTINCT currency_code) > 1'
  - name: employee_rowcount
    type: sql
    dimension: accuracy
    severity: info
    description: employee_rowcount
    query: SELECT COUNT(*) AS employee_rows FROM labor.employee
  - name: employee_role_distribution
    type: sql
    dimension: accuracy
    severity: info
    description: employee_role_distribution
    query: 'SELECT primary_role_code, COUNT(*) AS role_count

      FROM labor.employee

      WHERE is_current = TRUE

      GROUP BY primary_role_code'
- name: employee_availability
  physicalName: labor.employee_availability
  physicalType: table
  description: Associate availability and time-off windows. end must be after start; used as hard constraints
    by the schedule recommender.
  tags:
  - transactional
  authoritativeDefinitions:
  - url: canonical-core/labor/README.md
    type: businessDefinition
  - url: canonical-core/labor/glossary.md
    type: businessDefinition
  - url: docs/data-model-standards.md
    type: businessDefinition
  properties:
  - name: employee_availability_sk
    logicalType: integer
    physicalType: BIGINT
    required: true
    primaryKey: true
    primaryKeyPosition: 1
    description: Surrogate key.
  - name: employee_availability_id
    logicalType: string
    physicalType: STRING
    required: true
    description: Durable business key for the availability window.
  - name: employee_sk
    logicalType: integer
    physicalType: BIGINT
    required: false
    description: FK to employee.employee_sk (version-specific).
    relationships:
    - to: employee.employee_sk
      type: foreignKey
      customProperties:
      - property: toSchema
        value: labor
      - property: enforced
        value: false
  - name: employee_id
    logicalType: string
    physicalType: STRING
    required: true
    description: Durable pseudonymous employee business key.
    classification: restricted
    tags:
    - dbx_pii_identifier
  - name: availability_date
    logicalType: date
    physicalType: DATE
    required: true
    description: Local business date the window applies to (ISO 8601).
  - name: available_start_ts
    logicalType: timestamp
    physicalType: TIMESTAMP
    required: true
    description: Inclusive availability start (UTC, ISO 8601).
  - name: available_end_ts
    logicalType: timestamp
    physicalType: TIMESTAMP
    required: true
    description: Exclusive availability end (UTC, ISO 8601).
  - name: is_available
    logicalType: boolean
    physicalType: BOOLEAN
    required: true
    description: TRUE = can work this window; FALSE = explicitly unavailable (e.g. PTO block).
  - name: availability_type
    logicalType: string
    physicalType: STRING
    required: false
    description: 'Window type. Allowed values: preferred, available, unavailable, time_off.'
  - name: record_source
    logicalType: string
    physicalType: STRING
    required: false
    description: Originating system of record.
  - name: load_timestamp
    logicalType: timestamp
    physicalType: TIMESTAMP
    required: false
    description: When this row was loaded (UTC, ISO 8601).
  quality:
  - name: employee_availability_keys_not_null
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: employee_availability_keys_not_null
    query: "SELECT * FROM labor.employee_availability\nWHERE employee_availability_sk IS NULL OR employee_availability_id\
      \ IS NULL\n   OR employee_id IS NULL OR availability_date IS NULL\n   OR available_start_ts IS NULL\
      \ OR available_end_ts IS NULL OR is_available IS NULL"
  - name: employee_availability_pk_unique
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: employee_availability_pk_unique
    query: 'SELECT employee_availability_sk

      FROM labor.employee_availability

      GROUP BY employee_availability_sk

      HAVING COUNT(*) > 1'
  - name: employee_availability_biz_key_unique
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: employee_availability_biz_key_unique
    query: 'SELECT employee_availability_id, COUNT(*) AS n

      FROM labor.employee_availability

      GROUP BY employee_availability_id

      HAVING COUNT(*) > 1'
  - name: employee_availability_end_after_start
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: employee_availability_end_after_start
    query: 'SELECT * FROM labor.employee_availability

      WHERE available_end_ts <= available_start_ts'
  - name: employee_availability_fk_orphan
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: employee_availability_fk_orphan
    query: 'SELECT a.* FROM labor.employee_availability a

      LEFT JOIN labor.employee e ON a.employee_sk = e.employee_sk

      WHERE e.employee_sk IS NULL AND a.employee_sk IS NOT NULL'
  - name: employee_availability_rowcount
    type: sql
    dimension: accuracy
    severity: info
    description: employee_availability_rowcount
    query: SELECT COUNT(*) AS employee_availability_rows FROM labor.employee_availability
- name: employee_skill
  physicalName: labor.employee_skill
  physicalType: table
  description: Associate skill eligibility bridge. Role scheduling must not assign an employee lacking
    the required skill_code.
  tags:
  - transactional
  authoritativeDefinitions:
  - url: canonical-core/labor/README.md
    type: businessDefinition
  - url: canonical-core/labor/glossary.md
    type: businessDefinition
  - url: docs/data-model-standards.md
    type: businessDefinition
  properties:
  - name: employee_skill_sk
    logicalType: integer
    physicalType: BIGINT
    required: true
    primaryKey: true
    primaryKeyPosition: 1
    description: Surrogate key.
  - name: employee_skill_id
    logicalType: string
    physicalType: STRING
    required: true
    description: Durable business key (e.g. "<employee_id>|<skill_code>").
  - name: employee_sk
    logicalType: integer
    physicalType: BIGINT
    required: false
    description: FK to employee.employee_sk (version-specific).
    relationships:
    - to: employee.employee_sk
      type: foreignKey
      customProperties:
      - property: toSchema
        value: labor
      - property: enforced
        value: false
  - name: employee_id
    logicalType: string
    physicalType: STRING
    required: true
    description: Durable pseudonymous employee business key.
    classification: restricted
    tags:
    - dbx_pii_identifier
  - name: skill_code
    logicalType: string
    physicalType: STRING
    required: true
    description: 'Skill / qualification code. Allowed values: cashier, sales_associate, fulfillment, inventory,
      supervisor, food_handler, pharmacy_tech.'
  - name: proficiency_level
    logicalType: string
    physicalType: STRING
    required: false
    description: 'Proficiency. Allowed values: trainee, qualified, expert.'
  - name: is_active
    logicalType: boolean
    physicalType: BOOLEAN
    required: true
    description: TRUE when the skill is currently usable for scheduling.
  - name: effective_from_date
    logicalType: date
    physicalType: DATE
    required: true
    description: Inclusive date the skill became effective.
  - name: effective_to_date
    logicalType: date
    physicalType: DATE
    required: false
    description: Exclusive end date; NULL while active.
  - name: record_source
    logicalType: string
    physicalType: STRING
    required: false
    description: Originating system of record.
  - name: load_timestamp
    logicalType: timestamp
    physicalType: TIMESTAMP
    required: false
    description: When this row was loaded (UTC, ISO 8601).
  quality:
  - name: employee_skill_keys_not_null
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: employee_skill_keys_not_null
    query: "SELECT * FROM labor.employee_skill\nWHERE employee_skill_sk IS NULL OR employee_skill_id IS\
      \ NULL\n   OR employee_id IS NULL OR skill_code IS NULL OR is_active IS NULL\n   OR effective_from_date\
      \ IS NULL"
  - name: employee_skill_pk_unique
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: employee_skill_pk_unique
    query: 'SELECT employee_skill_sk

      FROM labor.employee_skill

      GROUP BY employee_skill_sk

      HAVING COUNT(*) > 1'
  - name: employee_skill_biz_key_unique
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: employee_skill_biz_key_unique
    query: 'SELECT employee_skill_id, COUNT(*) AS n

      FROM labor.employee_skill

      GROUP BY employee_skill_id

      HAVING COUNT(*) > 1'
  - name: employee_skill_fk_orphan
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: employee_skill_fk_orphan
    query: 'SELECT s.* FROM labor.employee_skill s

      LEFT JOIN labor.employee e ON s.employee_sk = e.employee_sk

      WHERE e.employee_sk IS NULL AND s.employee_sk IS NOT NULL'
  - name: employee_skill_rowcount
    type: sql
    dimension: accuracy
    severity: info
    description: employee_skill_rowcount
    query: SELECT COUNT(*) AS employee_skill_rows FROM labor.employee_skill
- name: employee_store_eligibility
  physicalName: labor.employee_store_eligibility
  physicalType: table
  description: Employee × store eligibility bridge for multi-location associates.
  tags:
  - transactional
  authoritativeDefinitions:
  - url: canonical-core/labor/README.md
    type: businessDefinition
  - url: canonical-core/labor/glossary.md
    type: businessDefinition
  - url: docs/data-model-standards.md
    type: businessDefinition
  properties:
  - name: employee_store_eligibility_sk
    logicalType: integer
    physicalType: BIGINT
    required: true
    primaryKey: true
    primaryKeyPosition: 1
    description: Surrogate key.
  - name: employee_store_eligibility_id
    logicalType: string
    physicalType: STRING
    required: true
    description: Durable business key (e.g. "<employee_id>|<store_id>").
  - name: employee_sk
    logicalType: integer
    physicalType: BIGINT
    required: false
    description: FK to employee.employee_sk (version-specific).
    relationships:
    - to: employee.employee_sk
      type: foreignKey
      customProperties:
      - property: toSchema
        value: labor
      - property: enforced
        value: false
  - name: employee_id
    logicalType: string
    physicalType: STRING
    required: true
    description: Durable pseudonymous employee business key.
    classification: restricted
    tags:
    - dbx_pii_identifier
  - name: store_sk
    logicalType: integer
    physicalType: BIGINT
    required: false
    description: FK to store.store_sk (version-specific).
    relationships:
    - to: store.store_sk
      type: foreignKey
      customProperties:
      - property: toSchema
        value: store
      - property: enforced
        value: false
  - name: store_id
    logicalType: string
    physicalType: STRING
    required: true
    description: Durable store business key the associate may work.
  - name: is_primary
    logicalType: boolean
    physicalType: BOOLEAN
    required: true
    description: TRUE when this is the primary / home eligibility row.
  - name: is_active
    logicalType: boolean
    physicalType: BOOLEAN
    required: true
    description: TRUE when the eligibility is currently usable.
  - name: effective_from_date
    logicalType: date
    physicalType: DATE
    required: true
    description: Inclusive eligibility start date.
  - name: effective_to_date
    logicalType: date
    physicalType: DATE
    required: false
    description: Exclusive eligibility end date; NULL while active.
  - name: record_source
    logicalType: string
    physicalType: STRING
    required: false
    description: Originating system of record.
  - name: load_timestamp
    logicalType: timestamp
    physicalType: TIMESTAMP
    required: false
    description: When this row was loaded (UTC, ISO 8601).
  quality:
  - name: employee_store_eligibility_keys_not_null
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: employee_store_eligibility_keys_not_null
    query: "SELECT * FROM labor.employee_store_eligibility\nWHERE employee_store_eligibility_sk IS NULL\
      \ OR employee_store_eligibility_id IS NULL\n   OR employee_id IS NULL OR store_id IS NULL OR is_primary\
      \ IS NULL OR is_active IS NULL\n   OR effective_from_date IS NULL"
  - name: employee_store_eligibility_pk_unique
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: employee_store_eligibility_pk_unique
    query: 'SELECT employee_store_eligibility_sk

      FROM labor.employee_store_eligibility

      GROUP BY employee_store_eligibility_sk

      HAVING COUNT(*) > 1'
  - name: employee_store_eligibility_biz_key_unique
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: employee_store_eligibility_biz_key_unique
    query: 'SELECT employee_store_eligibility_id, COUNT(*) AS n

      FROM labor.employee_store_eligibility

      GROUP BY employee_store_eligibility_id

      HAVING COUNT(*) > 1'
  - name: employee_store_eligibility_store_fk_orphan
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: employee_store_eligibility_store_fk_orphan
    query: 'SELECT x.* FROM labor.employee_store_eligibility x

      LEFT JOIN store.store st ON x.store_sk = st.store_sk

      WHERE st.store_sk IS NULL AND x.store_sk IS NOT NULL'
  - name: employee_store_eligibility_employee_fk_orphan
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: employee_store_eligibility_employee_fk_orphan
    query: 'SELECT x.* FROM labor.employee_store_eligibility x

      LEFT JOIN labor.employee e ON x.employee_sk = e.employee_sk

      WHERE e.employee_sk IS NULL AND x.employee_sk IS NOT NULL'
  - name: employee_store_eligibility_rowcount
    type: sql
    dimension: accuracy
    severity: info
    description: employee_store_eligibility_rowcount
    query: SELECT COUNT(*) AS employee_store_eligibility_rows FROM labor.employee_store_eligibility
- name: labor_actuals
  physicalName: labor.labor_actuals
  physicalType: table
  description: Worked timecard intervals for associates. end after start; feeds adherence and labor-risk
    actuals.
  tags:
  - transactional
  authoritativeDefinitions:
  - url: canonical-core/labor/README.md
    type: businessDefinition
  - url: canonical-core/labor/glossary.md
    type: businessDefinition
  - url: docs/data-model-standards.md
    type: businessDefinition
  properties:
  - name: labor_actuals_sk
    logicalType: integer
    physicalType: BIGINT
    required: true
    primaryKey: true
    primaryKeyPosition: 1
    description: Surrogate key.
  - name: labor_actuals_id
    logicalType: string
    physicalType: STRING
    required: true
    description: Durable business key for the worked interval.
  - name: employee_sk
    logicalType: integer
    physicalType: BIGINT
    required: false
    description: FK to employee.employee_sk (version-specific).
    relationships:
    - to: employee.employee_sk
      type: foreignKey
      customProperties:
      - property: toSchema
        value: labor
      - property: enforced
        value: false
  - name: employee_id
    logicalType: string
    physicalType: STRING
    required: true
    description: Durable pseudonymous employee business key.
    classification: restricted
    tags:
    - dbx_pii_identifier
  - name: store_sk
    logicalType: integer
    physicalType: BIGINT
    required: false
    description: FK to store.store_sk (version-specific).
    relationships:
    - to: store.store_sk
      type: foreignKey
      customProperties:
      - property: toSchema
        value: store
      - property: enforced
        value: false
  - name: store_id
    logicalType: string
    physicalType: STRING
    required: true
    description: Durable store business key where time was worked.
  - name: labor_schedule_sk
    logicalType: integer
    physicalType: BIGINT
    required: false
    description: FK to labor_schedule.labor_schedule_sk when the punch maps to a planned shift.
    relationships:
    - to: labor_schedule.labor_schedule_sk
      type: foreignKey
      customProperties:
      - property: toSchema
        value: labor
      - property: enforced
        value: false
  - name: labor_schedule_id
    logicalType: string
    physicalType: STRING
    required: false
    description: Durable schedule business key when mapped.
  - name: role_code
    logicalType: string
    physicalType: STRING
    required: true
    description: 'Role worked. Allowed values: cashier, sales_associate, fulfillment, inventory, supervisor.'
  - name: department_or_zone
    logicalType: string
    physicalType: STRING
    required: false
    description: Department or zone worked (free STRING; common values include storewide, front_end, grocery,
      backroom).
  - name: business_date
    logicalType: date
    physicalType: DATE
    required: true
    description: Local business date of the worked interval (ISO 8601).
  - name: clock_in_ts
    logicalType: timestamp
    physicalType: TIMESTAMP
    required: true
    description: Inclusive clock-in (UTC, ISO 8601).
  - name: clock_out_ts
    logicalType: timestamp
    physicalType: TIMESTAMP
    required: true
    description: Exclusive clock-out (UTC, ISO 8601).
  - name: worked_hours
    logicalType: number
    physicalType: DECIMAL(8,2)
    required: true
    description: Paid / worked hours for the interval (non-negative).
  - name: actuals_status
    logicalType: string
    physicalType: STRING
    required: true
    description: 'Status. Allowed values: approved, pending, rejected, adjusted.'
  - name: record_source
    logicalType: string
    physicalType: STRING
    required: false
    description: Originating system of record.
  - name: load_timestamp
    logicalType: timestamp
    physicalType: TIMESTAMP
    required: false
    description: When this row was loaded (UTC, ISO 8601).
  quality:
  - name: labor_actuals_keys_not_null
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: labor_actuals_keys_not_null
    query: "SELECT * FROM labor.labor_actuals\nWHERE labor_actuals_sk IS NULL OR labor_actuals_id IS NULL\n\
      \   OR employee_id IS NULL OR store_id IS NULL OR role_code IS NULL\n   OR business_date IS NULL\
      \ OR clock_in_ts IS NULL OR clock_out_ts IS NULL\n   OR worked_hours IS NULL OR actuals_status IS\
      \ NULL"
  - name: labor_actuals_pk_unique
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: labor_actuals_pk_unique
    query: 'SELECT labor_actuals_sk

      FROM labor.labor_actuals

      GROUP BY labor_actuals_sk

      HAVING COUNT(*) > 1'
  - name: labor_actuals_biz_key_unique
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: labor_actuals_biz_key_unique
    query: 'SELECT labor_actuals_id, COUNT(*) AS n

      FROM labor.labor_actuals

      GROUP BY labor_actuals_id

      HAVING COUNT(*) > 1'
  - name: labor_actuals_end_after_start
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: labor_actuals_end_after_start
    query: 'SELECT * FROM labor.labor_actuals

      WHERE clock_out_ts <= clock_in_ts'
  - name: labor_actuals_hours_nonnegative
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: labor_actuals_hours_nonnegative
    query: 'SELECT * FROM labor.labor_actuals

      WHERE worked_hours < 0'
  - name: labor_actuals_employee_fk_orphan
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: labor_actuals_employee_fk_orphan
    query: 'SELECT a.* FROM labor.labor_actuals a

      LEFT JOIN labor.employee e ON a.employee_sk = e.employee_sk

      WHERE e.employee_sk IS NULL AND a.employee_sk IS NOT NULL'
  - name: labor_actuals_store_fk_orphan
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: labor_actuals_store_fk_orphan
    query: 'SELECT a.* FROM labor.labor_actuals a

      LEFT JOIN store.store st ON a.store_sk = st.store_sk

      WHERE st.store_sk IS NULL AND a.store_sk IS NOT NULL'
  - name: labor_actuals_schedule_fk_orphan
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: labor_actuals_schedule_fk_orphan
    query: 'SELECT a.* FROM labor.labor_actuals a

      LEFT JOIN labor.labor_schedule s ON a.labor_schedule_sk = s.labor_schedule_sk

      WHERE s.labor_schedule_sk IS NULL AND a.labor_schedule_sk IS NOT NULL'
  - name: labor_actuals_rowcount
    type: sql
    dimension: accuracy
    severity: info
    description: labor_actuals_rowcount
    query: SELECT COUNT(*) AS labor_actuals_rows FROM labor.labor_actuals
- name: labor_schedule
  physicalName: labor.labor_schedule
  physicalType: table
  description: Planned associate shifts. end after start; used as supply baseline and double-booking constraint.
  tags:
  - transactional
  authoritativeDefinitions:
  - url: canonical-core/labor/README.md
    type: businessDefinition
  - url: canonical-core/labor/glossary.md
    type: businessDefinition
  - url: docs/data-model-standards.md
    type: businessDefinition
  properties:
  - name: labor_schedule_sk
    logicalType: integer
    physicalType: BIGINT
    required: true
    primaryKey: true
    primaryKeyPosition: 1
    description: Surrogate key.
  - name: labor_schedule_id
    logicalType: string
    physicalType: STRING
    required: true
    description: Durable business key for the scheduled shift.
  - name: employee_sk
    logicalType: integer
    physicalType: BIGINT
    required: false
    description: FK to employee.employee_sk (version-specific).
    relationships:
    - to: employee.employee_sk
      type: foreignKey
      customProperties:
      - property: toSchema
        value: labor
      - property: enforced
        value: false
  - name: employee_id
    logicalType: string
    physicalType: STRING
    required: true
    description: Durable pseudonymous employee business key.
    classification: restricted
    tags:
    - dbx_pii_identifier
  - name: store_sk
    logicalType: integer
    physicalType: BIGINT
    required: false
    description: FK to store.store_sk (version-specific).
    relationships:
    - to: store.store_sk
      type: foreignKey
      customProperties:
      - property: toSchema
        value: store
      - property: enforced
        value: false
  - name: store_id
    logicalType: string
    physicalType: STRING
    required: true
    description: Durable store business key where the shift is worked.
  - name: role_code
    logicalType: string
    physicalType: STRING
    required: true
    description: 'Role for the shift. Allowed values: cashier, sales_associate, fulfillment, inventory,
      supervisor.'
  - name: department_or_zone
    logicalType: string
    physicalType: STRING
    required: false
    description: Department or zone label (e.g. front_end, grocery, backroom, storewide). Free STRING;
      common values include storewide, front_end, grocery, general_merchandise, backroom.
  - name: business_date
    logicalType: date
    physicalType: DATE
    required: true
    description: Local business date of the shift (ISO 8601).
  - name: shift_start_ts
    logicalType: timestamp
    physicalType: TIMESTAMP
    required: true
    description: Inclusive shift start (UTC, ISO 8601).
  - name: shift_end_ts
    logicalType: timestamp
    physicalType: TIMESTAMP
    required: true
    description: Exclusive shift end (UTC, ISO 8601).
  - name: scheduled_hours
    logicalType: number
    physicalType: DECIMAL(8,2)
    required: true
    description: Scheduled duration in hours (must be non-negative).
  - name: schedule_status
    logicalType: string
    physicalType: STRING
    required: true
    description: 'Status. Allowed values: draft, published, cancelled.'
  - name: schedule_source
    logicalType: string
    physicalType: STRING
    required: false
    description: 'Origin. Allowed values: workforce_system, manager, recommendation_accepted, other.'
  - name: record_source
    logicalType: string
    physicalType: STRING
    required: false
    description: Originating system of record.
  - name: load_timestamp
    logicalType: timestamp
    physicalType: TIMESTAMP
    required: false
    description: When this row was loaded (UTC, ISO 8601).
  quality:
  - name: labor_schedule_keys_not_null
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: labor_schedule_keys_not_null
    query: "SELECT * FROM labor.labor_schedule\nWHERE labor_schedule_sk IS NULL OR labor_schedule_id IS\
      \ NULL\n   OR employee_id IS NULL OR store_id IS NULL OR role_code IS NULL\n   OR business_date\
      \ IS NULL OR shift_start_ts IS NULL OR shift_end_ts IS NULL\n   OR scheduled_hours IS NULL OR schedule_status\
      \ IS NULL"
  - name: labor_schedule_end_after_start
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: labor_schedule_end_after_start
    query: 'SELECT * FROM labor.labor_schedule

      WHERE shift_end_ts <= shift_start_ts'
  - name: labor_schedule_hours_nonnegative
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: labor_schedule_hours_nonnegative
    query: 'SELECT * FROM labor.labor_schedule

      WHERE scheduled_hours < 0'
  - name: labor_schedule_pk_unique
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: labor_schedule_pk_unique
    query: 'SELECT labor_schedule_sk

      FROM labor.labor_schedule

      GROUP BY labor_schedule_sk

      HAVING COUNT(*) > 1'
  - name: labor_schedule_biz_key_unique
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: labor_schedule_biz_key_unique
    query: 'SELECT labor_schedule_id, COUNT(*) AS n

      FROM labor.labor_schedule

      GROUP BY labor_schedule_id

      HAVING COUNT(*) > 1'
  - name: labor_schedule_employee_fk_orphan
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: labor_schedule_employee_fk_orphan
    query: 'SELECT s.* FROM labor.labor_schedule s

      LEFT JOIN labor.employee e ON s.employee_sk = e.employee_sk

      WHERE e.employee_sk IS NULL AND s.employee_sk IS NOT NULL'
  - name: labor_schedule_store_fk_orphan
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: labor_schedule_store_fk_orphan
    query: 'SELECT s.* FROM labor.labor_schedule s

      LEFT JOIN store.store st ON s.store_sk = st.store_sk

      WHERE st.store_sk IS NULL AND s.store_sk IS NOT NULL'
  - name: labor_schedule_rowcount
    type: sql
    dimension: accuracy
    severity: info
    description: labor_schedule_rowcount
    query: SELECT COUNT(*) AS labor_schedule_rows FROM labor.labor_schedule
