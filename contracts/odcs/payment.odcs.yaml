# GENERATED by tools/generate_odcs.py — do not edit by hand.
# ODCS v3.1.0 (Bitol / Linux Foundation). OPTIONAL integration; ORDM's
# source of truth is the committed DDL / checks.sql / glossary.md.
# Regenerate: python tools/generate_odcs.py
apiVersion: v3.1.0
kind: DataContract
id: ordm-core-payment
name: ordm_payment
version: 1.0.0
status: draft
domain: payment
tags:
- ordm
- retail
- canonical-core
description:
  purpose: ORDM canonical-core domain 'payment'. Generated from the committed DDL, relationships, quality
    checks and glossary — ORDM's source of truth. ODCS is an optional integration; the model does not
    depend on it.
  usage: Optional. Consumable by contract-driven tools (e.g. DQX for data-quality execution, Lockstep
    for Unity Catalog sync, Ontos as data products). See docs/odcs-integration.md.
authoritativeDefinitions:
- url: canonical-core/payment/README.md
  type: businessDefinition
- url: canonical-core/payment/glossary.md
  type: businessDefinition
- url: docs/data-model-standards.md
  type: businessDefinition
schema:
- name: payment
  physicalName: payment.payment
  physicalType: table
  description: ORDM canonical-core payment fact (sale / refund / adjustment / chargeback). Tender + settlement
    side of customer orders.
  tags:
  - transactional
  authoritativeDefinitions:
  - url: canonical-core/payment/README.md
    type: businessDefinition
  - url: canonical-core/payment/glossary.md
    type: businessDefinition
  - url: docs/data-model-standards.md
    type: businessDefinition
  properties:
  - name: payment_sk
    logicalType: integer
    physicalType: BIGINT
    required: true
    primaryKey: true
    primaryKeyPosition: 1
    description: Surrogate key.
  - name: payment_id
    logicalType: string
    physicalType: STRING
    required: true
    description: Durable natural/business key for the payment event.
  - name: order_id
    logicalType: string
    physicalType: STRING
    required: false
    description: FK to customer_order_line.order_id (the order this payment settles).
  - name: profile_sk
    logicalType: integer
    physicalType: BIGINT
    required: false
    description: FK to profile.profile_sk (version-specific).
    relationships:
    - to: profile.profile_sk
      type: foreignKey
      customProperties:
      - property: toSchema
        value: customer
      - property: enforced
        value: false
  - name: profile_id
    logicalType: string
    physicalType: STRING
    required: true
    description: Durable customer business key (pseudonymous; not direct PII).
  - name: payment_type
    logicalType: string
    physicalType: STRING
    required: false
    description: 'Direction/kind of the event. Allowed values: sale, refund, adjustment, chargeback.'
    quality:
    - type: text
      dimension: validity
      severity: error
      description: 'Allowed values: sale, refund, adjustment, chargeback.'
  - name: payment_method
    logicalType: string
    physicalType: STRING
    required: false
    description: 'Tender used. Allowed values: card, cash, wallet, bank_transfer, gift_card, voucher.'
    quality:
    - type: text
      dimension: validity
      severity: error
      description: 'Allowed values: card, cash, wallet, bank_transfer, gift_card, voucher.'
  - name: payment_status
    logicalType: string
    physicalType: STRING
    required: false
    description: 'Settlement status. Allowed values: authorized, captured, settled, declined, refunded,
      voided.'
    quality:
    - type: text
      dimension: validity
      severity: error
      description: 'Allowed values: authorized, captured, settled, declined, refunded, voided.'
  - name: amount
    logicalType: number
    physicalType: DECIMAL(18,2)
    required: false
    description: Event amount (magnitude, >= 0), in the reporting/base currency. payment_type carries
      the direction.
    quality:
    - type: text
      dimension: validity
      severity: error
      description: CHECK (amount >= 0).
  - name: currency_code
    logicalType: string
    physicalType: STRING
    required: false
    description: Reporting/base currency of ALL monetary columns, ISO 4217 alpha-3. Equals the deploy
      base_currency; single-currency by construction.
  - name: transaction_currency_code
    logicalType: string
    physicalType: STRING
    required: false
    description: Original tender currency before normalization (lineage only), ISO 4217 alpha-3. Convert
      with fx_rate.
  - name: event_timestamp
    logicalType: date
    physicalType: TIMESTAMP
    required: false
    description: Instant the payment event occurred (UTC, ISO 8601) — the event-time, distinct from load_timestamp
      (processing-time).
  - name: record_source
    logicalType: string
    physicalType: STRING
    required: false
    description: Originating system of record (vendor-neutral label).
  - name: load_timestamp
    logicalType: date
    physicalType: TIMESTAMP
    required: false
    description: When this row was loaded into the canonical core (UTC, ISO 8601).
  quality:
  - name: payment_keys_not_null
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: payment_keys_not_null
    query: "SELECT * FROM payment.payment\nWHERE payment_sk IS NULL OR payment_id IS NULL OR profile_id\
      \ IS NULL\n   OR payment_type IS NULL OR amount IS NULL"
  - name: payment_id_unique
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: payment_id_unique
    query: 'SELECT payment_id, COUNT(*) AS rows

      FROM payment.payment

      GROUP BY payment_id

      HAVING COUNT(*) > 1'
  - name: payment_type_domain
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: payment_type_domain
    query: 'SELECT * FROM payment.payment

      WHERE payment_type NOT IN (''sale'', ''refund'', ''adjustment'', ''chargeback'')'
  - name: payment_method_domain
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: payment_method_domain
    query: "SELECT * FROM payment.payment\nWHERE payment_method IS NOT NULL\n  AND payment_method NOT\
      \ IN ('card', 'cash', 'wallet', 'bank_transfer', 'gift_card', 'voucher')"
  - name: payment_status_domain
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: payment_status_domain
    query: "SELECT * FROM payment.payment\nWHERE payment_status IS NOT NULL\n  AND payment_status NOT\
      \ IN ('authorized', 'captured', 'settled', 'declined', 'refunded', 'voided')"
  - name: payment_amount_nonnegative
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: amount is a magnitude; direction is carried by payment_type.
    query: 'SELECT * FROM payment.payment

      WHERE amount < 0'
  - name: payment_profile_businesskey_orphan
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: payment_profile_businesskey_orphan
    query: "SELECT p.payment_id FROM payment.payment p\nLEFT JOIN (SELECT DISTINCT profile_id FROM customer.profile)\
      \ c\n  ON p.profile_id = c.profile_id\nWHERE c.profile_id IS NULL\nGROUP BY p.payment_id"
  - name: payment_order_orphan
    type: sql
    dimension: accuracy
    severity: warning
    mustBe: 0
    unit: rows
    description: Most payments settle a known order; adjustments may not, so this is a warning.
    query: "SELECT p.order_id FROM payment.payment p\nLEFT JOIN (SELECT DISTINCT order_id FROM orders.customer_order_line)\
      \ o\n  ON p.order_id = o.order_id\nWHERE p.order_id IS NOT NULL AND o.order_id IS NULL\nGROUP BY\
      \ p.order_id"
  - name: payment_single_reporting_currency
    type: sql
    dimension: accuracy
    severity: error
    mustBe: 0
    unit: rows
    description: payment_single_reporting_currency
    query: 'SELECT COUNT(DISTINCT currency_code) AS distinct_currencies

      FROM payment.payment

      HAVING COUNT(DISTINCT currency_code) > 1'
